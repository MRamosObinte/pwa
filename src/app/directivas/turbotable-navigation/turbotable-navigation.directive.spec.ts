import { TurbotableNavigationDirective } from './turbotable-navigation.directive';

describe('TurbotableNavigationDirective', () => {
  it('should create an instance', () => {
    const directive = new TurbotableNavigationDirective();
    expect(directive).toBeTruthy();
  });
});
