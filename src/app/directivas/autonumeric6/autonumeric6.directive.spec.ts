import { Autonumeric6Directive } from './autonumeric6.directive';

describe('Autonumeric6Directive', () => {
  it('should create an instance', () => {
    const directive = new Autonumeric6Directive(null);
    expect(directive).toBeTruthy();
  });
});
