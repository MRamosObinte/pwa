import { EnteroPositivoDirective } from './entero-positivo.directive';

describe('EnteroPositivoDirective', () => {
  it('should create an instance', () => {
    const directive = new EnteroPositivoDirective();
    expect(directive).toBeTruthy();
  });
});
