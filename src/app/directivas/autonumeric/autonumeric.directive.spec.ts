import { AutonumericDirective } from './autonumeric.directive';

describe('AutonumericDirective', () => {
  it('should create an instance', () => {
    const directive = new AutonumericDirective(null);
    expect(directive).toBeTruthy();
  });
});
