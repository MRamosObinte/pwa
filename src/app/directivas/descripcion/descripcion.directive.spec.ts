import { DescripcionDirective } from './descripcion.directive';

describe('DescripcionDirective', () => {
  it('should create an instance', () => {
    const directive = new DescripcionDirective(null);
    expect(directive).toBeTruthy();
  });
});
