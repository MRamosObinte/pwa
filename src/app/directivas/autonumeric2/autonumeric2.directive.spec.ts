import { Autonumeric2Directive } from './autonumeric2.directive';

describe('Autonumeric2Directive', () => {
  it('should create an instance', () => {
    const directive = new Autonumeric2Directive(null);
    expect(directive).toBeTruthy();
  });
});
