import { Equal2listDirective } from './equal-2list.directive';

describe('Equal2listDirective', () => {
  it('should create an instance', () => {
    const directive = new Equal2listDirective();
    expect(directive).toBeTruthy();
  });
});
