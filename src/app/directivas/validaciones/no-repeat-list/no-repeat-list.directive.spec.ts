import { NoRepeatListDirective } from './no-repeat-list.directive';

describe('NoRepeatListDirective', () => {
  it('should create an instance', () => {
    const directive = new NoRepeatListDirective();
    expect(directive).toBeTruthy();
  });
});
