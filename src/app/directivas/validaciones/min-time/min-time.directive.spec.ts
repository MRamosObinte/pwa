import { MinTimeDirective } from './min-time.directive';

describe('MinTimeDirective', () => {
  it('should create an instance', () => {
    const directive = new MinTimeDirective(null);
    expect(directive).toBeTruthy();
  });
});
