import { MaxTimeDirective } from './max-time.directive';

describe('MaxTimeDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxTimeDirective(null);
    expect(directive).toBeTruthy();
  });
});
