import { MinNumberDirective } from './min-number.directive';

describe('MinNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new MinNumberDirective(null);
    expect(directive).toBeTruthy();
  });
});
