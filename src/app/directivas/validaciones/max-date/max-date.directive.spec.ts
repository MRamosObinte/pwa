import { MaxDateDirective } from './max-date.directive';

describe('MaxDateDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxDateDirective();
    expect(directive).toBeTruthy();
  });
});
