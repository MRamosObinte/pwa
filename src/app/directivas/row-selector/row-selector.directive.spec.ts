import { RowSelectorDirective } from './row-selector.directive';

describe('RowSelectorDirective', () => {
  it('should create an instance', () => {
    const directive = new RowSelectorDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
