import { LowercaseDirective } from './lowercase.directive';

describe('LowercaseDirective', () => {
  it('should create an instance', () => {
    const directive = new LowercaseDirective(null);
    expect(directive).toBeTruthy();
  });
});
