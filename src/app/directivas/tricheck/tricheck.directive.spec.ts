import { TricheckDirective } from './tricheck.directive';

describe('TricheckDirective', () => {
  it('should create an instance', () => {
    const directive = new TricheckDirective();
    expect(directive).toBeTruthy();
  });
});
