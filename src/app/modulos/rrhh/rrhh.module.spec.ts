import { RrhhModule } from './rrhh.module';

describe('RrhhModule', () => {
  let rrhhModule: RrhhModule;

  beforeEach(() => {
    rrhhModule = new RrhhModule();
  });

  it('should create an instance', () => {
    expect(rrhhModule).toBeTruthy();
  });
});
